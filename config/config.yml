tables:
  onlines:
    fields:
      id: 'string'
      guestKey: 'string'
      channelId: 'string'
      expiresAt: 'integer'
      lastSeen: 'integer'
    expireKey: 'expiresAt'
    hashKey: 'channelId'
    rangeKey: 'id'
    subIndices:
      guest:
        hashKey: 'channelId'
        rangeKey: 'lastSeen'
